apiVersion: apps/v1
kind: Deployment

metadata:
name: self-healing-rag

spec:
replicas: 2

selector:
matchLabels:
app: rag

template:
metadata:
labels:
app: rag


spec:
  containers:

  - name: rag-api

    image: self-healing-rag:latest

    ports:
    - containerPort: 8000

