# Self-Healing-Rag
Staff-level GenAI platform that automatically detects retrieval failures and hallucinations, rewrites queries, and regenerates grounded responses using a self-healing feedback loop.
